import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ciabelarosa.netlify.app',
  appName: 'CiaBelaRosa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
